# Neon PostgreSQL connection string
DATABASE_URL=postgresql://user:password@ep-xxxx.ap-southeast-2.aws.neon.tech/melbourne_liveability?sslmode=require

# Mapbox token (free tier — needed for map tiles in frontend)
NEXT_PUBLIC_MAPBOX_TOKEN=your_mapbox_token_here
